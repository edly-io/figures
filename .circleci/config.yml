version: 2
jobs:
  build:
    # directory where steps are run
    working_directory: ~/circleci-figures
    docker:
      # CircleCI Python images available at: https://hub.docker.com/r/circleci/python/
      - image: circleci/python:3.8
        environment:
          BASH_ENV: ~/.bashrc
    steps: # steps that comprise the `build` job
      - checkout # check out source code to working directory
      - restore_cache:
          key: figures-test-deps-{{ checksum "devsite/requirements/py38_sumac.txt" }}
      - run:
          name: "Install python requirements"
          command: |
            sudo pip install --upgrade pip
            sudo pip install virtualenv
            virtualenv venv
            . venv/bin/activate
            pip install -r devsite/requirements/py38_sumac.txt
      - save_cache:
          key: figures-test-deps-{{ checksum "devsite/requirements/py38_sumac.txt" }}
          paths:
            - "venv"
      - run:
          name: "Run Tests"
          command: |
            . venv/bin/activate
            make sumac.pytest
      - store_test_results:
          path: test-results
      - store_artifacts:
          path: test-results
          destination: tr
